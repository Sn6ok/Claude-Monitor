<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Дозволи зведені до мінімуму (Частина 4 §17 Master Prompt).
        Застосунок нічого не читає з телефона: не має доступу до камери,
        мікрофона, розташування чи сховища.

        Мережа — щоб отримувати стан задач. Сповіщення й фоновий сервіс —
        лише для сповіщень про задачі: їх вмикає сам користувач
        у налаштуваннях, і без цього сервіс не запускається.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />

    <application
        android:name=".MonitorApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.ClaudeMonitor">

        <!--
            allowBackup=false навмисно: резервна копія могла б винести
            за межі пристрою дані, пов'язані з ідентичністю. Сам приватний
            ключ лежить в Android Keystore і з телефона не вивозиться
            у жодному разі, але й решту стану копіювати немає потреби.

            usesCleartextTraffic не вмикається: незашифрований трафік
            заборонений (Частина 3 §2, §23 Master Prompt).
        -->

        <!-- singleTop: дотик до сповіщення відкриває задачу в уже відкритому вікні. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Тримає з'єднання, поки ввімкнені сповіщення про задачі. -->
        <service
            android:name=".MonitorService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />
    </application>

</manifest>
